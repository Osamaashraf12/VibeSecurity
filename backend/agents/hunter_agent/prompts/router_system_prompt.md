# Hunter Agent Router System Prompt
# ========================
# PLACEHOLDER — Populate only when Hunter Agent implementation begins.
# This prompt will instruct the router model (qwen/qwen3.6-plus:free) to:
#   1. Ingest the full crawl dump
#   2. Classify endpoints by vulnerability domain
#   3. Produce a routing table of data slices for each sub-agent
